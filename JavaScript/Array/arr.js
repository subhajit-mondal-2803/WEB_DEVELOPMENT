// Array

// index      0   1   2   3   4
const age = [10, 23, 25, 34, 19];

// console.log(age[2]);

// Methods

const arr = [10, 20, 30, 40, 50];

// arr.push(60); // insert element in the end
// arr.pop(); // Remove from end
// arr.unshift(5);  // insert from begining
// arr.shift(); // remove from begining
// arr.splice(2, 1); // splice( startIndex,deleteCount,insertElement)
console.log(arr.length);

// arr.splice(arr.length, 0, 60, 70); // It adds at that specified index
console.log(arr);
